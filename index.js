const potencia = (base, expoente) => base ** expoente
const maiorValor = (n1, n2) => Math.max(n1, n2)
const menorValor = (n1, n2) => Math.min(n1, n2)
const raizQuadrada = (n1) => Math.sqrt(n1)

export {
    potencia,
    maiorValor,
    menorValor,
    raizQuadrada,
}